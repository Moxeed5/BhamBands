@model IEnumerable<Event> // Change this to match the actual model returned by GetEventsAsync

<h2>Events</h2>

@foreach (var eventItem in Model)
{
    <div>
        <h3>@Html.DisplayFor(modelItem => eventItem.name)</h3>
        <!-- Display other properties as needed -->
    </div>
}

